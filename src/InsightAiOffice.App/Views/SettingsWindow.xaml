<Window x:Class="InsightAiOffice.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="/Resources/InsightAiOffice.ico"
        Title="設定 / Settings" Width="420" Height="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource BgPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" x:Name="HeaderText" Text="設定 / Settings" FontSize="18" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,20"/>

        <!-- Language -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock x:Name="LanguageLabel" Text="言語 / Language" FontSize="13" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8"/>
            <ComboBox x:Name="LanguageCombo" Width="200" HorizontalAlignment="Left" FontSize="13"
                      SelectionChanged="LanguageCombo_SelectionChanged">
                <ComboBoxItem Content="日本語" Tag="ja"/>
                <ComboBoxItem Content="English" Tag="en"/>
            </ComboBox>
            <TextBlock x:Name="RestartHint" Text="" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,6,0,0"/>
        </StackPanel>

        <!-- AI Settings -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock x:Name="AiSettingsLabel" Text="AI 設定" FontSize="13" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8"/>
            <TextBlock x:Name="AiSettingsDesc" Text="AI プロバイダー・モデル・API キーを設定"
                       FontSize="11.5" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,8"/>
            <Button x:Name="OpenAiSettingsBtn" Content="AI 設定を開く" Padding="12,6" FontSize="13" Cursor="Hand"
                    Click="OpenAiSettings_Click" HorizontalAlignment="Left"
                    Background="{DynamicResource BgCardBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
        </StackPanel>

        <!-- Close -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CloseButton" Content="閉じる / Close" Padding="16,8" FontSize="13" Cursor="Hand"
                    Click="Close_Click"
                    Background="{DynamicResource PrimaryBrush}" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
